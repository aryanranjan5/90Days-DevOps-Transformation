# Day 2 - Users, Permissions, Processes

## Commands Practiced
whoami
id
groups
sudo adduser testuser
sudo usermod -aG sudo testuser
ps aux
top
kill <pid>

## Learnings
Users and groups control system access.
Processes are running programs in Linux.
Each process has a PID (Process ID).
kill command stops a running process.
